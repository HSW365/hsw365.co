# inventory_guardian.py uses only the Python standard library
# (urllib, smtplib, json, email) — no third-party dependencies required.
# This file is kept as a placeholder in case future automations need one.
